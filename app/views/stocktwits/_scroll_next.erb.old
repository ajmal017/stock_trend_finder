<script type="text/javascript">
var loading = false;
function load_next(symbol, max_id)
{
    //alert('loading!');
    loading = true;
    $('.last-twit-placeholder').show();


//    $.post(
//            <%= raw "'/stocktwits/next?" + (!request.env['QUERY_STRING'].empty? ? request.env['QUERY_STRING'] + '&' : '' ) + "max='" %> + $('table.stocktwits tr').filter(':last').attr('id'),
//            function(data){
//                if (data != "") {
//                    $('table.stocktwits tr').filter(':last').after(data);
//                }
//                $('.last-twit-placeholder').hide();
//                loading = false;
//            }
//    );
};

$(window).scroll(function(){
    if($(window).scrollTop() >= $(document).height() - $(window).height() - 500) {
        if(!loading) {
            load_next();
        }
    }
});
</script>