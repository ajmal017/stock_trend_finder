=render "note_dialog"

.col-md-9
  #header
    #title
      %h1
        =link_to('StockTwits Archive', stocktwits_path, class: 'no-decoration')
        =link_to(image_tag(asset_path('icon-cloud-download.png')), stocktwits_refresh_path)
    #user-dropdown
      .btn-group
        %button.btn.btn-default.dropdown-toggle#screens-dropdown-button{:type=> "button", "data-toggle"=>"dropdown", "aria-expanded"=>"false"}
          ="2 Screens"
        %ul.dropdown-menu{ role: "menu" }
          %li= link_to("1 Screen", "", id: "screens-one-toggle")
          %li= link_to("2 Screens", "", id: "screens-two-toggle")
  #stocktwits-watch-indicator{ "data-symbol"=>"" }
    =link_to(image_tag(asset_path('icon-star-sm-yellow.png'), class: 'stocktwits-watch-indicator', style: "display: none;"), "#", id: 'toggle-watching')
  #stocktwits-filter-indicator{ "data-symbol"=>"" }
    &nbsp;
  #stocktwits-add-twit
    %button.btn.btn-default#add-twit-button{ type: 'button' } New Note

  #stocktwits-content
    %table.stocktwits
      -@twits.each do |twit|
        =render "twit", twit: twit

  .last-twit-placeholder
    =image_tag(asset_path("loading.gif"))

.col-md-3
  .stocktwits-ticker-navigation
    = render partial: 'ticker_navigation'
  .stocktwits-setup-navigation
    = render partial: 'setup_navigation'

=render "toggle_single_screen_script"
=render "load_twits_script"
=render "edit_note_dialog_script"
=render "add_twit_dialog_script"
