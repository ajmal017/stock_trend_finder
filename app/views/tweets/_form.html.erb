<%= form_for(@tweet) do |f| %>
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% @tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div><%= f.text_area(:message, style: "width: 800px;") %></div>

  <div><%= image_tag(@tweet.local_image_url, style: "width: 800px") %></div>
  <%=f.hidden_field :local_image_url %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
